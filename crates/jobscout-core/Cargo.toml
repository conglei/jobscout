[package]
name = "jobscout-core"
description = "Search, retrieval, and ranking over the open-jobs dataset."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true

[dependencies]
